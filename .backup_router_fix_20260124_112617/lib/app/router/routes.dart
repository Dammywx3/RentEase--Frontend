class AppRoutes {
  // auth
  static const login = '/login';
  static const register = '/register';

  // shells (bottom nav entrypoints)
  static const tenantShell = '/tenant';
  static const agentShell = '/agent';
}
